#include<iostream>

int main() {
	int T;
	long long int N,x,M;
	scanf("%d", &T);
	while(T) {
		scanf("%lld %lld %lld", &N, &x, &M);
		long long int
		while (i < ARRAY_SIZE && scanf("%d", &a[i++]) == 1)
		T--;
	}
}
